import Logo from '../assets/Logo'
import Navigation from './Navigation'

const Header = () => {
    return (
        <nav class="main-nav">
            <Logo />
            <Navigation />
        </nav>
    )
}

export default Header
